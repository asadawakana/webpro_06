<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>数比べ</title>
</head>
<body>
    <p>あなたの手は<%= your %>です．</p>
    <p>コンピュータは<%= cpu %>です．</p>
    <p>判定：<%= judgement %></p>
    <p>現在<%= total %>試合中、<%= win %>勝しています．</p>


    <form action="/random">
        <label for="hand">次に出す数字を入力: </label>
        <input type="number" name="hand" min="1" max="6" required>
        <input type="hidden" name="win" value="<%= win %>">
        <input type="hidden" name="total" value="<%= total %>">
        <input type="submit" value="比べる">
    </form>
</body>
</html>
